<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="toiduHind.MainToiduFol.HomePage.HomePage"
             BackgroundColor="White">

    <Grid RowDefinitions="Auto, *">

        <!-- Верхняя панель -->
        <Grid BackgroundColor="Black" Padding="15,50,15,15" Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ImageButton Source="menu.png"
                         x:Name="MenuButton"
                         Clicked="OnMenuButtonClicked"
                         BackgroundColor="Transparent"
                         WidthRequest="30"
                         HeightRequest="30"
                         Margin="0,0,10,0"/>

            <Label x:Name="GreetingLabel"
                   Grid.Column="1"
                   Text="Tere, {name}"  
                   TextColor="White"
                   FontSize="20"
                   VerticalTextAlignment="Center"/>

        </Grid>

        <!-- Основной контент -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="15" Spacing="15">

                <!-- Первый Expander -->
                <toolkit:Expander>
                    <toolkit:Expander.Header>
                        <Frame BackgroundColor="#F0F0F0"
                               CornerRadius="5"
                               HasShadow="True"
                               Padding="10"
                               Margin="0">
                            <Label Text="Discounts in stores"
                                   FontSize="18"
                                   TextColor="Black"/>
                        </Frame>
                    </toolkit:Expander.Header>
                    <Label Text="List of discounts..." Padding="10"/>
                </toolkit:Expander>

                <!-- Второй Expander -->
                <toolkit:Expander>
                    <toolkit:Expander.Header>
                        <Frame BackgroundColor="#F0F0F0"
                               CornerRadius="5"
                               HasShadow="True"
                               Padding="10"
                               Margin="0">
                            <Label Text="Show nearby stores"
                                   FontSize="18"
                                   TextColor="Black"/>
                        </Frame>
                    </toolkit:Expander.Header>
                    <Label Text="Nearby stores..." Padding="10"/>
                </toolkit:Expander>

            </VerticalStackLayout>
        </ScrollView>

        <!-- Затемнение -->
        <BoxView x:Name="Overlay"
                 BackgroundColor="Black"
                 Opacity="0"
                 IsVisible="False"
                 InputTransparent="True"
                 Grid.RowSpan="2">
            <BoxView.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnOverlayTapped"/>
            </BoxView.GestureRecognizers>
        </BoxView>

        <!-- Меню -->
        <Frame x:Name="SlidingMenu"
               BackgroundColor="#292929"
               Padding="15"
               WidthRequest="250"
               VerticalOptions="FillAndExpand"
               TranslationX="-250"
               HorizontalOptions="Start"
               CornerRadius="0"
               HasShadow="True"
               Opacity="0.95"
               Grid.RowSpan="2">

            <VerticalStackLayout Spacing="15">

                <!-- Заголовок Menu с иконкой настроек -->
                <Grid ColumnDefinitions="*,Auto" Padding="0,0,0,10">
                    <Label Text="Menu"
               TextColor="White"
               FontAttributes="Bold"
               FontSize="22"
               VerticalOptions="Center"
               HorizontalOptions="Start"/>

                    <ImageButton Source="settings.png"
                     WidthRequest="24"
                     HeightRequest="24"
                     BackgroundColor="Transparent"
                     Clicked="OnSettingsClicked"
                     HorizontalOptions="End"
                     VerticalOptions="Center"
                     Margin="0,0,5,0"/>
                </Grid>

                <!-- Разделитель -->
                <BoxView HeightRequest="1" BackgroundColor="Gray" Opacity="0.5"/>

                <!-- Кнопки категорий -->
                <Button Text="🥩 Lihad" BackgroundColor="Transparent" TextColor="White" FontSize="18" Clicked="OnOptionSelected"/>
                <Button Text="🐟 Kalad" BackgroundColor="Transparent" TextColor="White" FontSize="18" Clicked="OnOptionSelected"/>
                <Button Text="🥚 Munad" BackgroundColor="Transparent" TextColor="White" FontSize="18" Clicked="OnOptionSelected"/>

            </VerticalStackLayout>


        </Frame>

    </Grid>

</ContentPage>
